=begin

=UndirectedHashGraph クラス

Hash を使って実装した、普通の無向グラフ。

==スーパークラス:

* ((<DirectedHashGraph|URL:DirectedHashGraph.html>))

==クラスメソッド:

--- UndirectedHashGraph.new(vertices= [], edges= [], params= {})
    
    頂点 ((|vertices|)) 、辺 ((|edges|)) を持つ無向グラフを作ります。
    
    ((<DirectedHashGraph|URL:DirectedHashGraph.html>)).new を参照してください。

==メソッド:

((<DirectedHashGraph|URL:DirectedHashGraph.html>)) と同じです。
